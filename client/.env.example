# Base URL of the Express API (server.js) — must match its CORS-allowed origin.
# All axios requests are prefixed with this and the "/api" path already used
# by every backend route (server.js mounts routers at /api/...).
VITE_API_URL=http://localhost:5000/api
